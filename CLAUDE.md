# CLAUDE.md

このリポジトリで作業するAI（Claude Code）への指示。

## プロジェクト概要

「まなびの広場」— 小6〜中1向けの算数・数学学習サービス。RPG風のステータス成長で
継続学習を促す。詳細は [README.md](./README.md)（使う人向け）と
[DESIGN.md](./DESIGN.md)（設計）を参照。

- バックエンド: Ruby on Rails 8 (API mode) + PostgreSQL（`backend/`）
- フロントエンド: React + TypeScript + Vite（`frontend/`）
- デプロイ: バックエンド=Railway / フロントエンド=Vercel

## ドキュメント更新ルール（重要）

**機能・データモデル・API・主要ロジックを変更したら、同じコミットで
[DESIGN.md](./DESIGN.md) を必ず更新する。** 実装とドキュメントを乖離させない。

具体的には、以下を変更したら DESIGN.md の該当セクションも直す:

- テーブル/カラムの追加・変更 → 「2. データモデル」（ER図含む）
- ポイント/ボーナス/ノルマ/成長曲線などの計算ロジック → 「3. 主要ロジック」
- APIエンドポイントの追加・変更 → 「4. API一覧」
- 画面/ルートの追加・変更 → 「5. 画面遷移」「6. フロント構成」

セットアップ手順や環境変数を変えたら README.md も更新する。

機能を「後回し」にしたら [TODO.md](./TODO.md) に追記し、着手・完了したらその行を消す。

## 開発フロー

- 変更後は必ずフロントの型チェック＆ビルドを通す: `cd frontend && npm run build`
- バックエンドはローカルにDBがない前提。構文チェックとルート確認で代替:
  `cd backend && ruby -c <file>` / `bundle exec bin/rails routes`
- コミットは意味のある単位で。変更が完了したら main にプッシュ（Railway/Vercelが自動デプロイ）。
- DBマイグレーションはデプロイ時に自動実行される（`bin/docker-entrypoint`）。

## 設計上の約束

- ステータスは `stat_types` テーブルで管理し、後から追加できる構造を保つ。
- 演習・問題集・テスト・復習はすべて `AnswerRecord` 経由でポイント加算する（経路を分けない）。
- **ポイントは回答時に確定して `answer_records.points_awarded` に保存する**（あとから再計算しない）。
  解き直しの逓減は「前回正解から14日」という時系列ルールなので、読み出し時に再計算すると
  過去の成長曲線まで書き変わる。加点ロジックは `AnswerRecord` の1箇所に集約する。
- 加点の出どころは3系統（回答・テストのボーナス・教材の読了）。**成長曲線では必ず全部を合算する**。
  どれかを落とすと折れ線が低く伸び、最後の「現在」だけ跳ね上がる。
- 問題セットの抽出は `ProblemScope`（範囲→問題）に集約する。両モードで共用。
- **練習の出題は「4段階優先度が先、習熟度（難易度）はその中での並べ替え」。** 順序を逆にして
  先に難易度で絞ると「未挑戦かつ難易度3」が無い状況で候補がゼロになる。
  習熟度は**その単元の直近10問の正答率**で測る（全体の正答率だと得意分野の数字で
  苦手分野にも難問が出る）。テスト・昇格試験は実力測定なので対象外。
- **難易度を使う機能は、コンテンツ側に幅があって初めて成立する。** 出題ロジックだけ足しても
  単元に難易度4・5が無ければ何も変わらない。ロジックと問題の追加はセットで考える。
- テストのfarming対策は「自己ベスト更新時のみボーナス」で行う。通常ポイントは再挑戦でも加算。
- 成長曲線の過去分は `answer_records` から再構築する（`student_stats` は現在値のみ）。
- 数式は KaTeX（`$...$` / 独立行の `$$`）。教材は `MarkdownView`、問題文・選択肢・ヒントは
  `MathText`（Markdownを解釈しない軽量版）で描画する。
- **生成AIの返答も数式を含む。** 記法は `claude_teacher.rb` のシステムプロンプトで
  「行内の `$...$` のみ・Markdown禁止」に固定し、`AskTeacher` で `MathText` に通す。
  指示が外れたとき用の正規化（`$$`→`$`、`**`除去）も `AskTeacher` 側に置く。
- **`seeds.rb` で LaTeX を書くときは必ず Ruby のリテラル文字列にする**
  （ヒアドキュメントは `<<~'MD'`、1行文字列はシングルクォート）。
  ダブルクォート／補間ありだと `\frac` が改ページ文字、`\times` がタブに化けて数式が壊れる。
- **既存の問題文を書き換えるときは、必ずデータ移行のマイグレーションを添える。**
  `seeds.rb` は `Problem.find_or_create_by!(question: ...)` と問題文をキーに探すため、
  文面だけ変えると seed が新しい問題を**別レコードとして作って重複する**（回答履歴も
  古い方に残る）。デプロイは `db:prepare` → `db:seed` の順なので、先に移行すれば防げる。
- 問題文は**表示だけ**数式化する。答え合わせは `problems.answer` の平文比較のまま。
  「（分数は a/b の形で答えること）」など**入力の書き方の説明は数式にしない**
  （子どもが打つ文字と見た目がずれるため）。
- **判定が平文比較である以上、答え方は問題文で明示する。** 単位のつく答えを求める問題には
  「（単位はつけず数字だけで答えること）」を必ず付ける（「135km」で不正解になるのを防ぐ）。
  単位だけ添える「（km/h）」のような書き方はしない。分数も絡むときは
  「（単位はつけず、分数は a/b の形で答えること）」と1文にまとめる。
- **`problems.hint` は解く前、`problems.solution` は間違えたあと**。役割が違うので混ぜない。
  解き方は不正解のときだけ返す・出す（正解した子に読ませない）。
- **`find_or_create_by!` のブロックは新規作成のときしか走らない。** あとから列を足して
  `seeds.rb` のデータに書くだけでは既存レコードに入らない。`fill_solution` のように
  作成後にも入れる経路を用意する（空のときだけ入れれば管理画面の編集も残る）。
- **手書きメモ（`ScratchPad`）のキャンバスには `touch-action: none` を必ず付ける。**
  無いとスマホで指を動かしたときに線を引くかわりにページがスクロールする。
  線は画像ではなく座標の配列で持つ（「もどす」と画面幅の変化に強い）。
  **メモの中身は保存せず、使い方の設定（開閉・手書き/文字・広さ）だけ localStorage に残す。**
  問題ごとに `key` で作り直す作りなので、設定まで消すと毎問押し直しになる。
- **記述式の答えは `AnswerInput`（入力欄＋キーパッド）で受ける。画面ごとに `<input>` を書かない。**
  スマホのソフトキーボードは `inputMode="none"` で出さず、キーパッドで打たせている。
  **答えに使う文字を増やしたら `KEYPAD_ROWS` にも足すこと**（無い文字はスマホから打てない）。
- 重い画面は `App.tsx` で `React.lazy` により遅延読み込みする（教材ページ・管理ページ）。
- **ルーティングは `react-router` v8。`react-router-dom` からは import しない**
  （v8 でそのパッケージは無くなった）。`BrowserRouter` なども `react-router` から取る。
- **BGMの曲は `sound.ts` の `BGM_TRACKS` に並べ、読むのは再生中の1曲だけ**（`preload="none"`＋
  再生時に `src` 差し替え）。曲を増やしても初回の通信量が増えない形を崩さない。
  保存するのは曲のIDでファイルパスではない（差し替えても設定が壊れないように）。
  **曲を固定しているときは `loop = true` にしてブラウザ標準のループに任せる**。
  `ended` で自前に回すのは「ランダム」のときだけでよく、経路を減らすほど自動再生制限に強い。
- **APIの応答にフィールドを足したら、フロントは `api/index.ts` で既定値を埋めてから返す。**
  フロント（Vercel）は数十秒、バックエンド（Railway）はビルドとマイグレーションで数分かかるので、
  デプロイのたびに「新フロント × 旧バックエンド」の窓ができる。素で `a.titles.length` のように
  読むと、その間だけ画面が真っ白になる（実際に `/stats` で起きた）。
  `badges: r.data.badges ?? []` のように入口1箇所で埋めておけば、応答の形が変わっても落ちない。
  逆にフィールドを**消す**ときは、フロントが参照しなくなってから消す（順番が逆だと同じ窓で壊れる）。
- **`render json: xs.map do |x| ... end` と書かない。** `do...end` は `map` ではなく `render` に
  結合するので、整形前の生レコード（`password_digest` を含む）がそのまま返る。
  `payload = xs.map do ... end` と変数に受けてから `render json: payload` する
  （`{ }` ブロックなら意図どおりに結合する）。
- **その日のノルマは `daily_quotas` に記録する。あとから復元できないため。**
  ノルマは `goals` と当時のステータス値から計算するが、`goals` は上書き更新で履歴を持たない。
  「ノルマを達成した日」の連続を数えるなら、その日のうちに残すしかない。ポイントの方は
  `points_awarded` が確定値なので記録しない（二重に持たない）。
  ノルマは**その日1回だけ決めて以後変えない**（日中に動くと達成判定がひっくり返る）。
- **総合ランクは「合計ポイントで到達 → 昇格試験に合格」で上がる。降格はしない。**
  落ちたときの再挑戦しきい値（`students.last_exam_points`）は、**採点が終わって加点が
  反映されたあと**に記録する。試験の回答も `AnswerRecord` を通ってポイントが入るので、
  先に記録すると試験自身の得点で条件を満たし、落ちた直後に再挑戦できてしまう。
- **バッジの定義はコード（`BadgeCatalog`）、獲得の記録はDB（`student_badges`）** に置く。
  条件が列で表せないため定義は持たせない。獲得日時を残すのは、取った瞬間を検出して
  お祝いを出すため（都度計算だけだと変化が分からない）。称号はバッジのアンロック制。
- **テスト・昇格試験の一括提出では空欄が来る**（時間切れの自動提出・飛ばし）。
  `AnswerRecord` は `submitted_answer` が presence 必須なので、そのまま渡すと500になる。
  `AnswerRecord.normalize_submitted` を通して「（未回答）」＝不正解として記録する。
- パスワードを変えると `generates_token_for :auth` の署名が変わり、発行済みトークンは失効する。
  本人がパスワードを変える経路では、応答で**新しいトークンを返して**フロントに差し替えさせる
  （そうしないと変更した瞬間にログアウトされる）。

## コードスタイル

- 周囲のコードに合わせる（コメント量・命名・書き方）。UI文言は日本語（対象が小中学生）。
- フロントの共通表示は `components/`（例: `ProblemView`, `GrowthChart`）に切り出して再利用する。
