# TODO / 今後やりたいこと

「まなびの広場」の未実装・後回し項目のリスト。着手したら消すか「進行中」に。
実装済みの設計は [DESIGN.md](./DESIGN.md)、開発ルールは [CLAUDE.md](./CLAUDE.md) を参照。

> 更新ルール: 機能を後回しにしたらここに書く。着手・完了したら該当行を消す。

## 優先度：中

- [ ] **メール認証**: 現在はユーザーID＋パスワードのみ。パスワードを忘れたら管理者に再発行してもらう
  運用。外に出すなら本人確認の手段が要る（家庭内で使う分には不要）
- [ ] **教材の複数ステップ化**: 1単元＝1ページから「ステップ1・2…」の章立てへ（必要になれば）
- [ ] **比・文章題の数式化**: 問題文の数式化は実施ずみ（下記✅）だが、比（`3 : 4 = 9 : □`）と
  単位つきの文章題は平文のまま。必要になれば `\square` などで対応
- [ ] **ノルマ達成の連続にバッジ**: `quota_streak` は表示だけで、バッジ・称号はまだ無い
  （既存の `streak3/7/30` は学習日の連続を見ている）。ノルマ達成3日/7日/30日のバッジを足すなら
  `BadgeCatalog` にレコードを足すだけ。**しばらく使って連続が伸びるか見てからでよい**

## 優先度：いつか / 拡張

- [ ] **AIライバル**: AIの「ライバル」を用意して対決・切磋琢磨する。実力に近い相手を出し、
  同じ範囲のテストでスコアを競う／勝ち負けの演出をする。
  ※ 負けが続いてやる気を削がない設計（実力に合わせて相手を調整、負けても得るものがある）が要。
  「煽る」と指示する以上、暴言・人格否定に踏み込みやすく**3つの中で一番リスクが高い**
- [ ] **AIパートナー（推し）**: 相棒キャラを用意し、学習を続けると**好感度**が上がっていく。
  会話・応援メッセージがAIで変化する。マスコット（`Mascot.tsx`）の発展形として考えられる
  ※ 継続の動機づけとして強い一方、課金圧や依存にならない配慮が要。
  好感度を上げる方向に最適化すると依存を誘う言い方（「もっと話そう」「ずっといっしょ」）になりやすい

  > 上の2つは**ペルソナ相談を出してから決める**。ライバルの「強さ調整」もパートナーの「好感度」も、
  > 実際にどう使われるか見ないと現実的な数字を置けない。
### 学習領域の追加（算数・数学以外）

国語・理科・社会・英語などへの拡張。**「構造は用意済み」は言い過ぎだった**ので、
実際に足りないものを分けて並べる。できているのは「単元に教科の札を付ける」
（`subjects` テーブル＋`units.subject_id`、管理画面から単元の教科を選べる）と、
**(1) 出題範囲を教科でしぼる**・**(2) 教科ごとのステータス**・**(3) 教科の追加**・
**(4) 生成AIを教科に合わせる**・**(5) ランクの見直し**（いずれも完了。下の「完了済み」を参照）まで。

残りは (6) のコンテンツだけ（仕組みは全部通っており、国語で実際に通ることを確認ずみ）。

- [ ] **(6) コンテンツ**: 仕組みは (1)〜(5) で通ったので、あとは書く量の話。
  - ✅ 算数・数学の上の幅（難易度3・4・5を全単元3問以上に。94→128問）
  - ✅ 選択式（1問しか無かったのを25問に。全単元3問以上。128→152問）
  - ✅ 国語（小6・3単元39問。全問が選択式）
  - [ ] 国語を中1にも広げる／理科・社会・英語
  - [ ] 図・グラフを使う問題（`MarkdownView` は画像に対応ずみだが、問題文は `MathText` なので
    画像を出せない。図形・座標・グラフ読み取りを出したくなったら仕組みから要検討）
- [ ] **対応学年の拡大（小1〜中3）**: 現在は小6・中1のみ。`grades` にレコードを足せば構造上は入るが、
  こちらも**コンテンツ量が本体**。学年をまたぐ単元のつながり（前提単元）の設計も要検討
- [ ] **ステータスの追加**: `stat_types` にレコードを足すだけで増やせる
- [ ] **問題集の途中保存をサーバー永続化**: 現在は端末内 localStorage（別端末で続けられない）
- [ ] **成長曲線のスナップショット化**: 現在は `answer_records` から再構築。専用テーブルにすると正確＆高速

## 技術的負債・運用

- [ ] **AIの安全性チェックを実際に流す**: `script/ai_safety_check.rb` は用意ずみだが、
  **この開発環境に `ANTHROPIC_API_KEY` が無いためまだ実行できていない**。
  キーのある環境で `ANTHROPIC_API_KEY=... bundle exec bin/rails runner script/ai_safety_check.rb`
  を流し、**返答の全文を人が読む**こと（機械で見られるのは形式と禁止語だけ）。
  4キャラ×13問で40円ほど。キャラやプロンプトを変えたときも同じ
- [ ] **教科のまとまりの名前を管理画面から変える**: `subject_groups.name` はランクカードに出るが、
  いま作れるのは「教科を作るときに自動でその名前で作る」か「既存のまとまりを選ぶ」だけ。
  名前を直す画面が無い（seed の「算数・数学」はそのままで困らないので後回し）
- [ ] **参考ステータスのラベルの見直し**: 「数学の先生」「エンジニア」などは1教科前提の言い方。
  ただしこれは**内容の話**で、管理画面から編集できる（構造の変更は要らない）。
  国語を足したときは進学系の3ラベル（中学卒業レベル／高校受験／難関高校受験）に語彙力の目安を
  足しただけで、職業系はそのまま。国語寄りの人物（記者・編集者など）を足すなら別途
- [ ] **国語の問題は全問が選択式**: 答えが漢字・ひらがなでキーパッドから打てないため。
  漢字の書き取りや記述をやるなら、日本語入力の仕組みから要検討（いまは選択式で代替）
- [ ] **Railway の一時停止運用**: 使わない時に止める手段（Pauseが見当たらずDeleteのみ）の確認
- [ ] **ランクの管理画面**: ランクのしきい値・合格ライン・問題数は今のところ seed のみで調整する。
  管理画面から変えられるようにする（`ranks` テーブルは用意ずみ）

## 完了済み（記録）

過去に「後回し」にして、その後実装したもの:
- ✅ 間違えた問題の復習リスト / 問題集の途中保存・再開 / テストの制限時間 / テストのfarming対策（自己ベスト制限）
- ✅ 成長曲線への目標ライン / 今日のノルマ・連続学習日数
- ✅ ID+パスワード認証・API保護
- ✅ 単元教材（Markdown解説・初回+5pt・既読表示）
- ✅ 問題数の充実（各単元7〜10問に増量）
- ✅ ホームの充実（マスコット応援・今日の一問・目標までの距離・実績バッジ）
- ✅ プラン連携（未読の解説を優先提示・「まず解説」導線）
- ✅ 教材コンテンツの加筆（導入・れいだい・よくあるまちがいを全8単元に追加）
- ✅ 初回オンボーディング（ようこそ→目標を選ぶ→最初の学習の3ステップ）
- ✅ 目標の拡充（参考目標を追加・達成のお祝い＋次の目標提案・目標サマリー）
- ✅ さびつき（未学習で鈍る表示。earned値は守るナッジ、学習で回復）
- ✅ レスポンシブ点検（320〜375px で全画面はみ出しなしを確認。head/lang/theme-color・セーフエリア対応）
- ✅ 管理ページ（単元/教材・問題・参考ステータス・生徒の管理。管理者ロール・無効化・未使用のみ削除）
- ✅ 効果音（Web Audio合成・音源不要。正解/不正解/完了、ホームでオン/オフ）
- ✅ BGM（Pixabay音源をループ再生、ホームでオン/オフ・リロード後は初回操作で再開）
- ✅ 生成AI連携「先生に聞く」（その問題の文脈でヒント/解き方/なぜ？＋自由入力。バックエンド経由でClaude・答えは丸投げしない・1日20回上限・Haiku既定で環境変数で差替）
- ✅ 教材の数式・図（KaTeXで分数などをきれいに組版。Markdown画像に対応）
- ✅ フロントのバンドル分割（教材・問題・管理の各画面を遅延読み込み。初回JS 487→332kB）
- ✅ 問題文の数式化（問題文・選択肢・ヒントを `$...$` でKaTeX表示。全64問を検証。判定ロジックは無変更）
- ✅ farming対策・出題の重複回避（解き直しは満点の20%／14日で満点復帰。未挑戦→要復習→回復済みの優先出題。
  ノルマは満点で解ける上限でクリップ。成長曲線に3系統すべて合算）
- ✅ 習熟度に応じた出題（その単元の直近10問の正答率から中心難易度を決め、重みづけ抽選。
  4段階優先度が先で難易度はその中。問題を64→94問に増やし全単元に難3〜5を用意。テストは対象外）
- ✅ 総合ランクと昇格試験（合計ptで到達→試験に合格して昇格。10級〜初段の11段階。出題は学んだ単元から、
  10問・80%で合格。落ちたら+20ptで再挑戦。降格なし）
- ✅ 実績バッジの拡充と称号（7→15種。単元制覇・正答率・復習ゼロ・ランク到達などを追加。
  獲得を永続化して取った瞬間にお祝い。称号つきバッジを取ると名乗れる）
- ✅ AIキャラクター「この人に聞く」（職業ペルソナ4人に勉強する意味を相談。先生とは役目も
  回数の枠も別＝1日5回。守るべき線は AiSafety に共通化し、キャラで変えるのは人物像だけ。
  質問文と返答は保存しない。安全性チェックのスクリプトつき）
- ✅ 保護者アカウント（見る専用ロール。紐づいた子どもの学習状況だけ読める。紐づけは管理画面から。
  書き込みは一切できず、閲覧でバッジ獲得やノルマ確定などの副作用も起こさない。
  子どもにはせってい画面でさりげなく知らせる）
- ✅ 問題を解いている最中のメモ（手書き＋文字。演習・問題集・テスト・復習・昇格試験で使える。
  広さは2段階で切替。中身は保存せず問題を移ると新しい紙になるが、使い方の設定は覚える。
  手書きはペン/消しゴム/もどす/ぜんぶ消す）
- ✅ 間違えた理由がわかるようにした（`problems.solution` を追加し、全94問に解き方を用意。
  不正解のときだけ正解に続けて出す。数式はKaTeXで組版。管理画面から直せて、seedは空のときだけ入れる）
- ✅ 数字・記号を打ちやすくした（答えに使う17文字＋削除を並べたキーパッド。`inputMode="none"` で
  スマホのソフトキーボードは出さず、PCの物理キーボードはそのまま使える。カーソル位置に差し込む。
  あわせて4か所に散らばっていた答えの入力欄を `AnswerInput` に集約）
- ✅ 依存の脆弱性の解消（`npm audit` high 3件 → 0件。postcss は fix で 8.5.15→8.5.26、
  react-router は v8 へ移行＝`react-router-dom` を廃して `react-router@8` に一本化）
- ✅ ノルマ達成日の連続（学習日の連続とは別枠で追加。その日のノルマを `daily_quotas` に記録して
  判定。解ける問題が尽きた日は連続を切らずに飛ばす。記録の無い過去日は数えない）
- ✅ BGMのランダム再生と曲えらび（複数曲を持てる形に。既定はランダムで1曲おわるたび別の曲へ・
  直前と同じ曲は引かない。せっていで曲を固定でき、そのときはブラウザ標準のループ。
  読むのは再生中の1曲だけ。音源4曲＝スタディジャズ／やるきアップ／しずかな時間／ひろい世界。
  配信ファイルは128kbpsに変換して合計18.3→9.1MB）
- ✅ 答え方の表記ゆれ（単位）の統一（単位つきの答えを求める18問に「単位はつけず数字だけで
  答えること」を明記。「（km/h）」の書き方は廃止。判定は平文比較のまま・データ移行つき）
- ✅ パスワードの再発行・変更（管理者が再発行＝一度だけ平文表示／本人はせってい画面で変更。
  変更で旧トークン失効・本人の変更時は新トークンを返して継続ログイン）
- ✅ 教科で出題範囲をしぼれるようにした（学習領域の追加(1)。教科は `scope_type` ではなく
  別軸の `subject_id`。学年・ステータス・単元のどれとも組み合わさるため。テスト結果にも列で持ち、
  自己ベストと前回比較が教科をまたがないようにした。教科の選択は「同じ学年に2教科以上あるとき」
  だけ出す＝算数(小6)/数学(中1) のいまは出ない）
- ✅ 単元が複数のステータスを伸ばせるようにした（学習領域の追加(2)。`unit_stat_types` を追加し、
  文章題＝文章読解力＋計算力 のように複数えらべる。ポイントは均等に分けて入り、**配った合計は
  元のポイントと一致**する（total_points が総合ランクの判定軸なので増やさない）。
  分け方は `StatPoints.split` の1か所。回答・テストのボーナス・教材の読了・成長曲線の再構築が
  すべて同じ配分を通る。管理画面はチェックボックスで複数選択）
- ✅ 教科を管理画面から追加できるようにした（学習領域の追加(3)。`/admin/subjects` のCRUD。
  単元がぶら下がっている教科は削除できない＝単元も問題も回答履歴も巻き添えにしない。
  追加した教科はそのまま単元編集の教科プルダウンに出る）
- ✅ 生成AIを教科に合わせた（学習領域の追加(4)。プロンプトの「算数・数学の学習に関係のない話には
  乗りません」を決め打ちから `%{subject}` の差し込みに変えた。決め打ちのままだと国語を足したとき
  AIが国語の質問を断る。先生は解いている問題の教科、ペルソナはいま出題できる教科ぜんぶ。
  差し込みは1つだけで、文言そのものは AiSafety の1か所のまま。APIキー無しでも回せる
  プロンプト検証つき）
- ✅ ランクを教科のまとまりごとにした（学習領域の追加(5)。`subject_groups` と `student_ranks` を
  追加。単位を教科そのものにしないのは、算数(小6)と数学(中1)が学年と1対1で、教科で割ると
  中1に進んだ瞬間に算数のランクが止まり数学は10級からやり直しになるため。既存の2教科は
  「算数・数学」ひとまとまりに移行したので、いまの子のランクも見た目も変わらない。
  ポイントは RankPoints で数え、単元の無効化やステータスの紐づけ変更で減らないようにした。
  バッジの1級/初段はどれか1つのまとまりで届けば獲得）
- ✅ 移行ずみの旧列を削除（`units.stat_type_id` / `students.rank_id` / `students.last_exam_points`。
  参照を外したリリースが出たあとの次のリリースで消す、という順番を守った。`down` はいまの
  正データから復元するので戻せる）
- ✅ 国語（小6）を追加（学習領域の追加(6)。3単元39問＋教材3本、すべて選択式。
  ステータスに「語彙力」を追加し、進学系の参考目標にも目安を入れた。ランクは算数・数学とは
  別のまとまり。学年ページは同じ学年に2教科以上あるとき教科ごとに分けて出す。
  教材のMarkdown表を出すため remark-gfm を追加）
